import type { ReactElement } from "react";
import AppShell from "./_app-shell";

/** Landing page using the Miro application shell. */
export default function HomePage(): ReactElement {
  return <AppShell />;
}
